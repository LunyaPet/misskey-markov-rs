default:
  image: rust:latest

stages:
  - test

test:
  stage: test
  script:
    - cargo build --release
    - cargo test --release
  dependencies:
    - build:arm64
  artifacts:
    paths:
      - target/release/markovify-rs
